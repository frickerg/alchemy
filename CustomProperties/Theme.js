import { DefaultTheme } from 'react-native-paper';

const theme = {
  ...DefaultTheme,
  roundness: 30,
  colors: {
    ...DefaultTheme.colors,
    primary: '#1bbc9c',
    light: '#bbebe1',
    subtitle: '#607D8B'
  }
};

export default theme;
